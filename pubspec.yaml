name: remote_database
description: "Paquete para el manejo de bases de datos remotas."
version: 1.0.2
homepage:

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  fpdart: ^1.1.1
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  supabase_flutter: ^2.9.1

dev_dependencies:
  build_runner: ^2.5.4
  flutter_test:
    sdk: flutter
  freezed: ^3.1.0
  json_serializable: ^6.9.5
  mockito: ^5.4.6
  very_good_analysis: ^9.0.0
